import { screen } from '@testing-library/react'
import { renderTheme } from '../../src/styles/theme/render-theme.tsx'
import {{pascalCase name}} from '.'

describe('<{{pascalCase name}} />', () => {
  it('should ', () => {
    renderTheme(<{{pascalCase name}} />)
  })
})
